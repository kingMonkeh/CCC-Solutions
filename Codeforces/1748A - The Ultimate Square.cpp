#include <bits/stdc++.h>

int main(){

    int tests;
    std::cin >> tests;
    while (tests--){

        double n;
        std::cin >> n;

        int ans = ceil(n / 2);
        std::cout << ans << '\n';
        
    }
    
    return 0;
    
}